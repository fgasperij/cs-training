#include <iostream>
#include <vector>

using namespace std;

int main() {
    // cumulative XOR
    // just try all intervals for each starting possibility
    // index by value the index of last occurence of that value
    // will be O(n^2)
}